<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kz.qbox.sdk.sample">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name="kz.qbox.sdk.sample.SampleApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QBox">

        <!-- Entry Point -->
        <activity
            android:name="kz.qbox.sdk.sample.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- socket -->
        <activity android:name="kz.qbox.sdk.sample.samples.socket.SocketActivity" />

        <!-- webrtc -->
        <activity android:name="kz.qbox.sdk.sample.samples.webrtc.WebRTCActivity" />
        <activity android:name="kz.qbox.sdk.sample.samples.webrtc.PreviewActivity" />

    </application>

</manifest>